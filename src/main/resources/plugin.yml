name: MiraEvents
version: ${version}
main: com.mira.events.MiraEventsPlugin
api-version: '1.21'
authors: [FiveS]
description: Central Mira event, announcement and safe restart scheduler.
depend: [MiraCore]
softdepend: [PlaceholderAPI, MiraShop, MiraKits, MiraItems, MiraTags, MiraCrates, MiraPinata, MiraNPC]

commands:
  miraevent:
    description: MiraEvents administration and status.
    aliases: [mevent, mevents]
    usage: /mevent <list|info|start|stop|announce|restart|reload>

permissions:
  miraevents.use:
    default: true
  miraevents.admin:
    default: op
